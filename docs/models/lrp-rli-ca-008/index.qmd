---
title: "LRP-CA-008: Concurrent conditional associations — skills → basic concepts (per wave)"

format:
  html:
    code-fold: true
    theme: cosmo
    number-sections: true
    toc: true
    toc-depth: 3
    code-links:
      - repo
---

{{< include _partials/_header.qmd >}}
{{< include _partials/_setup.qmd >}}
{{< include _partials/_gate_badge.qmd >}}
{{< include _partials/_key_findings.qmd >}}
{{< include _partials/_reading_guide.qmd >}}

## Overview

LRP-CA-008 extends the **concurrent conditional-associations** family (#312, descriptive-association workstream #314) beyond its original reading/vocabulary core set to **basic concepts** (CELF, `F`) as the focal outcome — a scope addition (#371) so the levels panel of the association matrix covers all non-floored outcomes. At each timepoint (t1–t4) it fits a between-child Beta-Binomial regression of basic concepts's level on the standardised same-wave logits of the full core skill set — word reading (`W`), letter sounds (`L`), phoneme blending (`B`), taught receptive (`TR`) and expressive (`TE`) vocabulary, and standardised receptive (`R`) and expressive (`E`) vocabulary — plus age, the #371 trait covariates (non-verbal ability, hearing, speech, phonological memory), and a group nuisance term. The four waves are fitted separately and reported side by side.

The estimand is the mutually-adjusted association at each wave: *"at wave t, among children alike on age and the other skills, +n of a predictor skill is associated with +m more basic-concepts items"*. `F` sits outside the family's original core predictor set, so it enters as a focal outcome without any core measure being removed; it is an asymmetric, focal-only scope extension — `F` is not itself added to the sibling models' predictor sets. It is not floored, so it enters on the same footing as the core-set measures. All children are on the intervention from t2, so these associations describe the conditional joint distribution of the skill measures within a treated system.

_Estimand: descriptive association (no causal warrant)._ Every coefficient is an **adjusted association**. Conditioning on contemporaneous (post-treatment) skill levels is intentional and licensed *because nothing here is read causally*. Read the results with the Table-2-fallacy, measurement-error and collinearity/regularisation caveats set out in the Results section.

## Design

- **Four separate cross-sectional fits** (one row per child per wave), reported side by side — not one stacked model with a child random intercept, which would tilt the coefficients toward a within-child quantity and change the estimand (issue #312 decision). The wave with the largest complete-outcome sample (ties → latest) is the operational diagnostic anchor for the standard trace and plots; this does not make it substantively primary or establish that it is best-powered. Every adjusted and bivariate fit has complete convergence metrics in the tables.
- **Predictors** enter as standardised same-wave logits with regularising `Normal(0, 0.3)` slopes (essential at n ≈ 53 with a collinear predictor cluster). The full core skill set enters (`F` lies outside it, so none is removed). Floored measures (`P`, `N`) are excluded as predictors.
- **Trait covariates** aligned with the gains panel — non-verbal ability, hearing, speech and phonological memory — enter as t1 baselines broadcast across the waves (#371).
- **Group** is included only as a non-interpretable nuisance term (absorbs arm composition at each wave); it is not reported as an association.
- **Missing predictor values** are mean-imputed (0 on the standardised scale). This can bias a conditional coefficient, with no guaranteed direction when missingness relates to the predictor, outcome or other skills; observed and imputed counts are reported. Rows missing the focal *outcome* are dropped.

![Model diagram](model_graph.png){#fig-model .lightbox fig-align="left"}

{{< include _partials/_priors.qmd >}}
{{< include _partials/_prior_predictive.qmd >}}
{{< include _partials/_results_concurrent.qmd >}}
{{< include _partials/_technical.qmd >}}
{{< include _partials/_footer.qmd >}}
