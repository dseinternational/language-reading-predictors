---
title: "LRPDID014: Waitlist-crossover arm-by-wave contrasts for APT expressive information (EI)"

format:
  html:
    code-fold: true
    theme: cosmo
    number-sections: true
    toc: true
    toc-depth: 3
    code-links:
      - repo
---

{{< include _partials/_header.qmd >}}
{{< include _partials/_setup.qmd >}}
{{< include _partials/_gate_badge.qmd >}}
{{< include _partials/_key_findings.qmd >}}
{{< include _partials/_reading_guide.qmd >}}

::: {.callout-note}
Drafted by a LLM-based AI tool (Claude Code/Opus 5).
:::

## Overview

The Action Picture Test (Renfrew 1997) is an expressive-language measure that the project's causal diagram already treats as a direct outcome of randomised assignment (`IG -> {EI, EG}`), but which had no confirmed test ceiling until the manual maxima were supplied on 2026-08-18 — Grammar 37, Information 40. Until then it sat under "Deferred / follow-ups" in the ITT suite design note, and no Bayesian estimate existed for either scale.

Information awards half marks on some items, so the raw 0–40 score is not an integer count and a Beta-Binomial likelihood rejects it. The modelled outcome is the **doubled half-mark scale, out of 80** (`aptinfo_x2`). Doubling is exact — every observed fractional part is 0.5 — and preserves the proportion, so the logit mean structure is unchanged. It does assert 80 exchangeable trials where there are 40 partial-credit items, which overstates per-child precision; the Beta-Binomial concentration absorbs part of that, and [LRPITT129](../lrp-rli-itt-129/) is the registered out-of-40 comparator.

LRPDID014 models the time 1–3 outcome levels with a saturated arm-by-wave structure and a child
random intercept. `tau_t2` is the clean randomised t2 contrast and is the quantity to set beside
the matching ITT fit; `arm_gap_t1` is a baseline-balance quantity, and `arm_gap_t3` and the
derived crossover contrast are **post-crossover associations** because by t3 both arms have been
taught. The model does not condition on the treatment-affected t2 score, and the child random
intercept partially pools stable heterogeneity rather than making each child their own control.

## Model

Bounded t1/t2/t3 levels are fitted jointly with separate immediate-minus-waitlist gaps at each
wave, a Beta-Binomial likelihood with $N = 80$, linear age, and a child random intercept.
Priors follow the shared template in `priors.py`.

![Model diagram](model_graph.png){#fig-model .lightbox fig-align="left"}

{{< include _partials/_priors.qmd >}}
{{< include _partials/_prior_predictive.qmd >}}
{{< include _partials/_results_did.qmd >}}
{{< include _partials/_technical.qmd >}}
{{< include _partials/_footer.qmd >}}
