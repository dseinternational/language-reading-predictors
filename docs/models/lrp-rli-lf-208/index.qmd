---
title: "LRPLF208: Factors associated with the level of TROG receptive grammar (RG) — randomised-window (t1/t2) comparator"

format:
  html:
    code-fold: true
    theme: cosmo
    number-sections: true
    toc: true
    toc-depth: 3
    code-links:
      - repo
---

{{< include _partials/_header.qmd >}}
{{< include _partials/_setup.qmd >}}
{{< include _partials/_gate_badge.qmd >}}
{{< include _partials/_key_findings.qmd >}}
{{< include _partials/_reading_guide.qmd >}}

## Overview

**This is a comparator, not the model of record.** It is [LRPLF08](../lrp-rli-lf-008/index.qmd) restricted to the two waves at which randomisation alone separates the arms — the pre-randomisation baseline t1 and the end of the randomised period t2 (#584 decision 3). Same model, same adjustment set, same estimand.

In the four-wave fit the post-crossover t3/t4 likelihood reaches the reported t2 change through parameters the waves share: the balance term `arm_gap_t1` the changes are measured from, the child random intercept, the dispersion, and the single time-invariant `group × ability` term. Across the level suite the posterior correlation between `arm_gap_t1` and `d_grp_time[t2]` runs from −0.07 to −0.44, so the contrast is randomisation-anchored but also longitudinal-model-dependent. Here there is nothing to borrow, and the difference between the two fits is the size of that dependence.

Read the four-wave report for the levels view across all four timepoints — showing them on one scale is that model's purpose, and this restriction gives it up.

LRPLF208 is a **level-factors** model for TROG receptive grammar (`trog`): what is associated with the score *level* at each of the four timepoints (Beta-Binomial logit, child random intercept; no own baseline).

_Causal labelling + level-model crossover caveat._ Every non-randomised coefficient is an **adjusted association** under the DAG. The group effect is `group x time`, parameterised as the covariate-adjusted arm gap at t1 (`arm_gap_t1`, a pre-randomisation *balance* quantity, never an effect) plus the change in that gap at each later wave (`d_grp_time[t]`; #552): after t2 the waitlist crosses over, so the **clean randomised contrast lives only at t2** — the t2 change `d_grp_time[t2]`, a difference-in-differences of adjusted levels, labelled *causal*; the only later wave in this fit is t2, so the model carries `arm_gap_t1` and `d_grp_time[t2]` alone — the post-crossover waves are not fitted here at all, which is the point of the comparator. SES excluded (non-DAG / redundant).

## Model

Beta-Binomial$(N, \mu\kappa, (1-\mu)\kappa)$ on the level count, $N = 32$, with
$\operatorname{logit}\mu_{t} = \alpha + \alpha_{\text{time}[t]} + b_{\text{grp}[t]}\text{group} + \gamma_A A_{\text{std},t} + g_{\text{ability}[t]}z(\text{blocks}) + \gamma_{\text{grp\_ability}}\,\text{group}\,z(\text{blocks}) + u_{\text{child}}$.

![Model diagram](model_graph.png){#fig-model .lightbox fig-align="left"}

{{< include _partials/model/level-arm-gap-window.qmd >}}

{{< include _partials/model/level-intercepts.qmd >}}

{{< include _partials/_priors.qmd >}}
{{< include _partials/_prior_predictive.qmd >}}
{{< include _partials/_results_factors.qmd >}}
{{< include _partials/_technical.qmd >}}
{{< include _partials/_footer.qmd >}}
