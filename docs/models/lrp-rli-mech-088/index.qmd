---
title: "LRP88: Mechanism model — taught receptive vocabulary (TR) -> word reading (WR)"

format:
  html:
    code-fold: true
    theme: cosmo
    number-sections: true
    toc: true
    toc-depth: 3
    code-links:
      - repo
---

{{< include _partials/_header.qmd >}}
{{< include _partials/_setup.qmd >}}
{{< include _partials/_gate_badge.qmd >}}
{{< include _partials/_key_findings.qmd >}}
{{< include _partials/_reading_guide.qmd >}}

## Overview

LRP88 estimates the adjusted association between taught receptive vocabulary and word
reading using *all three phase transitions* (t1$\to$t2, t2$\to$t3, t3$\to$t4) with
phase-specific intercepts — the "an additional $p$ taught spoken words is associated
with $o$ additional words read" dose-response asked for by #311 (descriptive-association
workstream #314).

_Linear mechanism (estimand note)._ The mechanism enters as a **linear slope** $\beta_{\text{mech}}$ on
$\text{logit}_*(TR_{\text{post}}, N_{TR})$, not the 1-D HSGP curve used elsewhere in
this family, following the vocabulary-exposure precedent (LRP56/57): the nonparametric
curve does not converge on vocabulary predictors at reporting tier, and DAG-required
adjusters are not dropped to buy convergence. The estimand is therefore the **linear**
$TR \to W$ adjusted association — a single slope, not a shape.

## Adjustment set

The model conditions on this DAG-motivated adjustment set to reduce confounding of the
$TR_{\text{post}}$--$W_{\text{post}}$ association:

Parents of $TR$ under the revised DAG (2026-07-10): $\{A, GA, HS, IG, IS, RW\}$.

- $G$ — group; always conditioned on via $\beta_G$.
- $A$ — age; conditioned on linearly (a $\gamma_A\,z(\text{age})$ term).
- $HS$ — hearing status (`hs` / `hs_missing`), a common cause of the vocabulary and
  code skills; blocks every $TR \leftarrow HS \to \{\dots\} \to W$ route at its root.
- $RW$ — phonological memory (`erbto` / `erbto_missing`); blocks
  $TR \leftarrow RW \to \{TE, RV, EV, PA, NW\} \to W$.
- $W_{\text{pre}}$ — baseline word reading (temporal precedence; not a collider).
- $TR$ has no measured skill parent, so no concurrent measure confounder enters.

_Intervention sessions (IS) — adjusted (since 2026-07-17, reversing #309)._ $IS \to TR$ and
$IS \to WR$ make session dose a genuine common cause of exposure and outcome, so the
model conditions on it (`attend`, the sessions delivered during the period, entering
as $\gamma_{\text{attend}}$). $IS$ is also treatment-affected ($IG \to IS$) and a collider
($IG \to IS \leftarrow GA$), and an earlier handling (#309) left it unadjusted for fear of
opening $IG \to IS \leftarrow GA \to W$; a formal d-separation check
(`notes/202607171700-mech-intervention-sessions-adjustment-collider-review.md`) shows
that concern does not bite here, because the model always conditions on the randomised
arm ($\beta_G$), which closes that path at $IG$ — with $GA$ held hypothetically,
$\{G, A, HS, IS, RW\}$ d-separates $TR$ from $WR$ in the backdoor graph, whereas without
$IS$ an observable backdoor stays open. Adding $IS$ moved the endpoint contrast modestly
down on the July 2026 fits (TR +10.1 → +9.0 items), so shared session dose had been
mildly inflating the slope; what remains is the sessions-adjusted association, still
covariation within a treated system.

$TE$ and $RV$ are *descendants* of $TR$ that also affect $W$; conditioning on them
would block legitimate indirect paths (e.g. $TR \to TE \to W$) and pull the slope
toward the direct-only component, so they are **not** in the adjustment set. $GA$
(general ability) is latent and cannot be conditioned on; the child random intercept
proxies its time-invariant part, so the slope stays an adjusted association. Note also
that taught-vocabulary variation is largely intervention-generated: the slope
describes covariation within a treated system.

### Interpretation caveats

- *No unmeasured confounder assumption* for $(TR_{\text{post}}, W_{\text{post}})$: strong and not testable; latent general ability $GA$ is the named violation direction (the $IS$ backdoor is closed by adjustment).
- $TR_{\text{post}}$ is measured with error; non-negligible error attenuates the slope toward zero.
- Across-phase pooling assumes the structural relationship is stable over time, and one common arm coefficient assumes the residual group contrast is too. No phase-specific fit is available: `phase_specific_mechanism` is rejected during run-plan resolution, because the pipeline's curve, items and steepest-interval artefacts and its power-scaling variables are all single-curve. Read the exposure-support table by period and arm instead, and treat a period whose exposure range differs markedly from the others as a place the pooled fit is extrapolating.

![Model diagram](model_graph.png){#fig-model .lightbox fig-align="left"}

{{< include _partials/_priors.qmd >}}
{{< include _partials/_prior_predictive.qmd >}}
{{< include _partials/_results_mechanism.qmd >}}
{{< include _partials/_technical.qmd >}}
{{< include _partials/_footer.qmd >}}
