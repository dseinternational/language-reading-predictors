---
title: "LRP105: Letter-sound → word reading, moderated by nonword decoding (NW)"

format:
  html:
    code-fold: true
    theme: cosmo
    number-sections: true
    toc: true
    toc-depth: 3
    code-links:
      - repo
---

{{< include _partials/_header.qmd >}}
{{< include _partials/_setup.qmd >}}
{{< include _partials/_gate_badge.qmd >}}
{{< include _partials/_key_findings.qmd >}}
{{< include _partials/_reading_guide.qmd >}}

## Overview

LRP105 is the decoding-route companion to [`lrp-rli-mech-104`](../lrp-rli-mech-104/) (#421 Tier 2; review note #424). The review's Q2 showed that a majority of the letter-sound → word-reading association does *not* run through measured nonword decoding; this model asks the moderation form: does letter-sound knowledge convert to word reading *more* strongly for children who can already decode nonwords (NW)? A positive `gamma_int` would say the alphabetic principle pays off once a child can apply it.

NW is a `MEASURES` symbol (the 6-item nonword count), so this is the plain `mech-071` pattern — the moderator enters as its standardised same-wave logit. The letter-sound curve `f_mech` stays nonparametric (word reading is not floored).

_Two sharp caveats (from #424)._ **NW is a mediator** of the letter-sound → word-reading path (`LS → NW → WR` under the revised DAG), so this moderation is **descriptive only** — conditioning on a mediator's level can also open a collider path; read it alongside Q2, never as a controlled effect. And **NW is heavily floored** (zero for ≈72/64/52/40% of children at t1–t4), so the interaction is floor-limited in the early waves and its power is weak; the off-floor-indicator variant Frank flags may be better-powered and is left for a follow-up. Every coefficient is an **adjusted association**, latent general ability unblocked; `gamma_int` is never causal. [`lrp-rli-mech-205`](../lrp-rli-mech-205/) is the no-interaction companion for the nested PSIS-LOO test.

## Model

$$
\eta = \alpha + \alpha_{\text{phase}} + \beta_G G
  + f^L(\operatorname{logit}_* L_{\text{post}})
  + \gamma_{\text{mod}} z(\operatorname{logit}_* \text{NW}_{\text{post}}) + \gamma_{\text{int}} z(\operatorname{logit}_* L_{\text{post}})\, z(\operatorname{logit}_* \text{NW}_{\text{post}})
  + \gamma_{\text{own}}\operatorname{logit}_*(W_{\text{pre}})
  + \gamma_A z(A) + \sum_c \gamma_c z(c) + u_{\text{child}},
$$

where the $z(c)$ are the letter-sound → word-reading confounders hearing (`hs`), intervention sessions (`attend`) and speech production (`deapp_c`), each with a missing-indicator. `gamma_int` is the tested quantity.

![Model diagram](model_graph.png){#fig-model .lightbox fig-align="left"}

{{< include _partials/_priors.qmd >}}
{{< include _partials/_prior_predictive.qmd >}}
{{< include _partials/_results_mechanism.qmd >}}
{{< include _partials/_technical.qmd >}}
{{< include _partials/_footer.qmd >}}
