# LRP59 — does the intervention raise reading via letter-sounds? (exploratory mediation)

> [!WARNING]
> These notes were generated by an AI tool. Model numbers are from the
> **reporting** config (6 chains × 6000 draws).

Date: 2026-06-16

A **first, exploratory attempt** at the ITT-phase mediation decomposition. The
original brief **deferred formal mediation by design**; this is an early pass with
a **single** mediator, not a definitive result — **LRP62 supersedes it as the
route estimate** (see Honest reading). Built on `main` (after the arviz migration
#68 merged), as its own branch `feature/lrp59-mediation` — a **new model family**
(mediation), independent of the LRP70s interaction stack.

## Question and design

How much of the intervention's effect on word reading (W) is associated **with**
its effect on letter-sound knowledge (L)? Decompose the total effect into the
natural indirect effect (NIE, via L) and natural direct effect (NDE, other paths).

- **Phase 0 only** (`phase_mode="itt"`, t1→t2): the only randomised window (group
  1 intervention, group 2 wait-list control; both treated after t2). One row per
  child, **n = 53**. `G = group−1` (G=0 intervention, G=1 control); reported
  intervention-helps (positive = raises reading).
- Joint Beta-Binomial model: mediator `logit(L_t2) ~ a0 + a_G·G + a_L·logit(L_t1)
  + a_C·C_t1`; outcome `logit(W_t2) ~ b0 + b_G·G + b_M·z(logit L_t2) +
  b_GM·G·z(L_t2) + b_W·logit(W_t1) + b_C·C_t1`. G×M interaction included.
- **NDE/NIE by counterfactual simulation from the posterior (g-formula)**, not a
  coefficient product (invalid on the logit scale): per draw, simulate L_t2 under
  each arm from the mediator model, push through the outcome model, average over
  the observed covariate distribution.
- **Adjustment {G, A, E, R, L_t1, W_pre}** at **baseline (t1)** — randomisation
  handles G→Y, G→M; we adjust the measured L→W confounders (age, expressive vocab
  E — LRP58 found it independently predicts W — receptive vocab R, baselines).
  Baseline (not post) values respect the cross-world assumption.

## Result (reporting; n = 53; 0 divergences, R-hat 1.00, ESS ~21k)

Intervention-helps direction, word-count scale (out of 90):

| quantity | mean | 95% CI | P(>0) |
| --- | ---: | --- | ---: |
| **Total** (intervention → reading) | **+2.92** | [0.53, 5.35] | 0.99 |
| **NIE** (via letter-sound) | **+1.83** | [0.46, 3.64] | **0.998** |
| NDE (other paths) | +1.09 | [−0.95, 3.16] | 0.85 |
| **proportion mediated** (NIE/Total) | **0.62** | [0.17, 1.89] | — (P(Total>0)=0.99) |

**Exploratory read (directionally consistent, not definitive):** the indirect path
(NIE) is credibly positive (P>0 = 0.998), the direct path (NDE) is not credible
(P>0 = 0.85), and the point estimate is **~62% mediated** — consistent with
letter-sounds carrying much of the effect, *but* on a single mediator with a wide
interval (see the caveats and LRP62 below before reading it as phonics-dominant).
Coefficients are sensible and reconcile with the programme: `a_G` = −0.59
(intervention raises letter-sound, cf LRP55 τ_L), `b_M` = +0.52 (letter-sound
raises reading, cf LRP58).

**Reconciliation with LRP52.** LRP59's Total (+2.92 words/90 ≈ +3.2pp) agrees in
**direction and order of magnitude** with LRP52's τ_W (+1.83pp ≈ +1.65 words;
both credibly positive, overlapping wide CIs). It is modestly larger here,
attributable to the different, mediation-specific **linear** adjustment set
(LRP52 used age + own-baseline GPs and all seven cross-baselines).

## Honest reading

- **Wide intervals (n = 53).** The proportion-mediated CI [0.17, 1.89] is broad;
  the >100% upper tail reflects draws where the direct effect is slightly negative
  (indirect carries "more than all" of a smaller total). The robust reading is the
  **NDE/NIE split** (indirect credible, direct not), not a precise percentage.
- **Under assumptions.** Causal only under no-unmeasured-L→W-confounding (the
  binding, unverifiable assumption — the main limitation, stated front-and-centre
  in the report), plus the cross-world/consistency/positivity assumptions.
  Randomisation secures the G→Y and G→M legs. Not assumption-free proof of
  mechanism.
- **Contemporaneous mediator and outcome — no temporal precedence.** M = L_t2 and
  Y = W_t2 are measured at the **same wave**; this decomposes a *within-wave*
  association, not a temporally-ordered L→W effect. Conditioning on baselines
  mitigates but does not establish that L precedes W.
- **Superseded by LRP62 for the route estimate.** The better-specified
  phonics-route composite (letter-sound + blending; **LRP62**) puts the route share
  **lower (~38%)** with the direct/residual path **larger** at the mean. The
  defensible conclusion across both is **mixed routes**, not phonics-dominant.
  Treat LRP59 as an exploratory first attempt with a single mediator.

This is an exploratory first pass at the active-ingredient question; it does **not**
by itself establish a phonics-dominant mechanism. LRP62 (the route composite) is
the basis for the route claim, and points to **mixed routes**.

## Engineering

- New **mediation family** (genuinely new code, not a moderator flag):
  `factories.build_mediation_model` (joint mediator+outcome, returns row-aligned
  `MediationData` for the simulation), `mediation.decompose` (the g-formula),
  `pipeline.fit_mediation`. `priors.b_path_prior` (Normal(0,1)) for the b-path.
  `lrp59.py` (kind=`mediation`), registered in `fit_statistical_model.py`.
- ITT-phase data prep, Beta-Binomial likelihood, suite priors (tau/gamma_own/
  gamma_cross/kappa/alpha), nutpie — all reused. No LOO (it's a decomposition,
  not a model bake-off); PPC on both the mediator and outcome likelihoods.
- `docs/models/lrp59/index.qmd` with the DAG, adjustment-set rationale, and the
  **assumptions section front-and-centre**. ruff + spellcheck clean; renders;
  15/15 factory/preprocessing tests pass (new factory additive).

## Follow-on (not this work)

LRP74 (age × treatment ITT); the interaction-phase synthesis write-up
(LRP71–73: contributing skills combine additively, raw interactions are
between-child confounds). LRP59 is an exploratory first pass at the mediation
question — **superseded by LRP62** for the route estimate.
