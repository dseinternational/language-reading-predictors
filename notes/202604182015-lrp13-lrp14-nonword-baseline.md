# LRP13 + LRP14 (non-word reading) — baseline + MAE tune

> [!WARNING]
> These notes were generated by an AI tool.

Date: 2026-04-18

Second of the LRP11–LRP22 construct-gap pairs (per
``notes/202604181930-lrp11-22-target-plan.md``). Fills the
phonological-decoding gap — `nonword` was used only as a
predictor until now.

## Construct

``nonword`` is an items-correct score from a non-word decoding
task (observed range 0–6 in this sample). Phonological decoding
is distinct from sight-word reading (`ewrswr`, LRP01/02) — it
isolates the ability to blend graphemes into phonemes without
semantic support. A late-emerging reading skill, characteristically
delayed in Down syndrome.

## Target distributions

| Target | n | min | max | median | mean | std | skew | pct zero |
|---|---:|---:|---:|---:|---:|---:|---:|---:|
| `nonword` (level) | 207 | 0 | 6 | 0 | 1.24 | 1.81 | **1.38** | **57%** |
| `nonword_gain` | 153 | −6 | 6 | 0 | 0.41 | 1.66 | 0.40 | **48%** |

Heavily floor-loaded — unusual shape. Most children in the sample
have not yet started to decode non-words reliably, and half show
no gain between timepoints.

## Baseline vs MAE-tuned (Optuna 150 trials each)

### LRP13 (`nonword_gain`, 34 predictors, n=153)

Tuner-inner CV MAE **0.9801 ± 0.3358**. Moderate regime: 108
trees, lr 0.067, num_leaves 42, max_depth 5, near-zero L1, weak
L2.

| Metric | Baseline (500t) | MAE-tuned (108t) |
|---|---:|---:|
| CV MAE | 1.144 | **0.994** |
| CV RMSE | 1.516 | **1.372** |
| CV R² | **−0.116** | **0.158** |
| CV MedAE | 0.943 | **0.733** |
| In-sample R² | 0.984 | 0.564 |

CV R² crosses negative → positive (a 0.27 jump — larger than
LRP11's or LRP07's). The floor-heavy target is genuinely
predictable once the tune is right.

### LRP14 (`nonword`, 32 predictors, n=215)

Tuner-inner CV MAE **0.8696 ± 0.3104**. Slow-many-trees regime:
156 trees, lr 0.042, num_leaves 19, max_depth 8, min_child 29
(large leaves for floor-heavy target).

| Metric | Baseline (500t) | MAE-tuned (156t) |
|---|---:|---:|
| CV MAE | 1.036 | **0.929** |
| CV RMSE | 1.460 | **1.350** |
| CV R² | 0.129 | **0.180** |
| CV MedAE | 0.689 | **0.504** |
| In-sample R² | 0.992 | 0.597 |

Modest R² gain (+0.05) but CV MedAE drops 0.19 — the floor-heavy
distribution caps achievable CV R² but the median prediction
tightens meaningfully. Lowest CV R² of any level model so far —
consistent with the target shape.

## Top tuned permutation importance

### LRP13 (`nonword_gain`)

1. `nonword` (0.661) — base level dominates
2. `erbnw` (0.062) — speech-error on nonwords
3. `ewrswr` (0.050) — word reading
4. `aptinfo` (0.050) — expressive information
5. `spphon` (0.030) — phonics
6. `yarclet` (0.026) — letter-sound
7. `age` (0.025)
8. `rowpvt` (0.023)
9. `agespeak` (0.015)
10. `erbword` (0.013)

**`erbnw` at rank 2**: speech errors on **nonwords** specifically
are the strongest non-base predictor of nonword-reading gains.
Natural construct pairing (same stimulus class). Reading-cluster
(`ewrswr`, `spphon`, `yarclet`) fills ranks 3–6.

### LRP14 (`nonword`)

1. **`spphon`** (0.105) — phonics is top
2. `ewrswr` (0.040) — word reading
3. `yarclet` (0.035) — letter-sound knowledge
4. `aptinfo` (0.018) — expressive information
5. `blending` (0.016) — phoneme blending
6. `yarcsi` (0.015) — YARC summary index
7. `aptgram` (0.011) — expressive grammar
8. `mumedupost16` (0.008)
9. `rowpvt` (0.008)
10. `b1reto` (0.007)

**Reading-cluster dominates completely.** Top three slots are
phonics, word reading, and letter-sound knowledge — the
phonological-reading network. `blending` (phoneme blending)
joins in the second tier. Unlike LRP10/LRP12, here vocabulary
does NOT dominate — nonword reading is firmly a phonological
construct, not a vocabulary construct. Clean contrast with the
language-domain level targets.

## Cross-model observation

The pair of phonological-reading models (LRP14 level and LRP13
gain) are **reading/phonology dominated** in importance — clean
construct contrast with LRP10 / LRP12 (vocabulary-dominated
language targets). This contrast strengthens the read-language
/ read-decoding distinction emerging across the suite.

The floor-heavy distribution caps achievable R² around 0.18 at
level — a real ceiling from the data, not a modelling
limitation. A log or quantile variant is a natural follow-up.

## Current state

- **`lrp13`** (primary, MAE-tuned on 34 predictors): CV MAE 0.994
  ± ?, CV R² 0.158 ± ?, CV MedAE 0.733. No outlier exclusion,
  n=153.
- **`lrp14`** (primary, MAE-tuned on 32 predictors): CV MAE 0.929
  ± ?, CV R² 0.180 ± ?, CV MedAE 0.504. No outlier exclusion,
  n=215.
- No feature-selection steps applied yet.

## Next-step candidates

- **Log-transformed variant** (`lrp14_log`, `lrp13_log`) — the
  floor-heavy shape is a strong signed-log motivation.
- **Select01** on both. Reading cluster (`spphon`, `ewrswr`,
  `yarclet`, `blending`, `nonword`) is internally correlated;
  a dcorr audit will identify which to keep.
- **Quantile α=0.5 objective** — CV MedAE is already low (0.504
  level) but quantile could tighten further.
