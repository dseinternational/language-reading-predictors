# Mechanism models: age GP dropped; documentation templates refreshed

> [!WARNING]
> These notes were generated by an AI tool.

Date: 2026-04-18

## Decisions

1. `build_mechanism_model`'s `use_age_gp` default flipped from `True`
   to `False`. Per-model specs (`lrp56.py`, `lrp57.py`, `lrp58.py`) set
   the flag explicitly so the new default is visible on inspection.
2. All seven Quarto model reports (`docs/models/lrp5{2..8}/index.qmd`)
   updated to reflect the current default specifications. HSGP prior
   panels stripped from ITT and mechanism reports where the GP is off
   by default; mechanism-report adjustment-set bullets updated to
   describe age as a linear-only confounder by default with a pointer
   to the sensitivity flag; LRP55 rewritten to describe the model as
   a *linear Beta-Binomial across eight outcomes with shared
   predictor structure and no random effects*.

## Mechanism-model refit comparison

Reporting config (6 chains × 6000 × 6000 × target_accept 0.95).

| Model | Variant | Divergences | f_mech__eta 95 % CI | Slope mean slope 95 % CI | Notable |
|---|---|---|---|---|---|
| LRP56 (R→W) | GP on | 268 / 36000 (0.74 %) | (0.000, 0.441) | +0.112 (−0.044, +0.377) | — |
| LRP56 (R→W) | **GP off** | **192 / 36000 (0.53 %)** | **(0.000, 0.379)** | **+0.064 (−0.065, +0.281)** | Slightly smaller slope, still non-credible |
| LRP57 (E→W) | GP on | 132 / 36000 (0.37 %) | (0.000, 0.576) | +0.196 (−0.013, +0.501) | — |
| LRP57 (E→W) | **GP off** | **247 / 36000 (0.69 %)** | **(0.000, 0.505)** | **+0.125 (−0.030, +0.404)** | Slope CI shifted toward zero |
| LRP58 (L→W) | GP on | 85 / 36000 (0.24 %) | (0.001, 0.621) | +0.137 (−0.002, +0.304) | — |
| LRP58 (L→W) | **GP off** | **4 / 36000 (0.01 %)** | **(0.077, 0.684)** | **+0.164 (+0.011, +0.320)** | **Amplitude and slope CIs now both exclude zero at 95 %** |

- **LRP58 is the star improvement.** Divergences drop from 0.24 % to
  essentially zero (4 / 36000), the `f_mech__eta` lower bound moves
  from 0.001 → 0.077 (firmly away from zero), and the mean-slope
  95 % CI (+0.011, +0.320) now also excludes zero. The L→W mechanism
  is the only one with a credibly non-zero slope on the logit scale.
- **LRP56 slightly cleaner** on divergences; mean slope shifted
  toward zero but CI unchanged in qualitative reading.
- **LRP57 divergences went up** (0.37 % → 0.69 %). Small absolute
  change; R̂ and ESS still clean. The posterior for E→W is still
  suggestive positive with CI spanning zero (mean slope +0.125 vs
  +0.196 previously; wider relative to the mean, so signal is
  effectively weaker after removing the age GP).

Taken together, the age-GP-off default is a clear win for LRP58 and
a small neutral-to-positive change for LRP56; LRP57 is a wash on
divergences but the LOO-supported simplification is justified on
model-parsimony grounds.

## Other diagnostics after refit

| Model | R̂ (max) | ESS_bulk min (scalar) | Pareto k > 0.70 |
|---|---|---|---|
| LRP56 | 1.002 | 3 305 | 0 / 157 |
| LRP57 | 1.001 | 3 840 | 0 / 157 |
| LRP58 | 1.001 | 5 656 | 0 / 157 |

All Pareto k ≤ 0.70 in every model (previously 1 % had one marginal
point). Clean.

## Confounder posteriors after refit

- **LRP57 γ_R**: 0.10 (95 % CI −0.18, +0.38). Moved from 0.15
  (−0.14, +0.44) with the GP on — slightly smaller, CI still spans
  zero.
- **LRP58 γ_E**: 0.21 (95 % CI −0.03, +0.45). Moved from 0.28
  (+0.02, +0.53) — now just-barely includes zero at 95 %. Was the
  sharpest confounder signal across the mechanism set; still
  suggestive but no longer "credibly positive".
- **LRP58 γ_R**: 0.02 (95 % CI −0.25, +0.29). Now essentially zero.
- All β_G still centred near zero (remember: pooled-phase mechanism
  models do not estimate a direct treatment effect — see the β_G
  caveat in `notes/202604181600-lrp52-58-findings.md`).

## Revised LRP55 report

`docs/models/lrp55/index.qmd` now reads:

> The default model is a **linear Beta-Binomial across eight outcomes
> with shared predictor structure and no random effects**: outcome-
> specific intercepts, slopes, own- and cross-baseline couplings, and
> outcome-specific dispersion. No Gaussian process on age, no LKJ
> residual correlation across outcomes.

A consolidated callout points to all three decision notes (the LKJ
drop note, the age-GP drop note, and the consolidated findings note)
so future readers can reconstruct why the final spec looks the way
it does.

## Refreshed comparison artefacts

`python scripts/compare_statistical_models.py --config reporting`
re-run, regenerating:

- `output/statistical_models/comparison/itt_vs_joint_tau.csv`
- `output/statistical_models/comparison/tau_forest.png`
- `output/statistical_models/comparison/mechanism_forest.png` and
  `mechanism_forest.csv`

The mechanism-forest plot now shows LRP58 with both endpoints on the
positive side of zero; LRP56 and LRP57 CIs still touch or span zero.

## Acceptance

- `ruff check src/ scripts/ tests/` — clean.
- `pytest tests/statistical_models/ -q` — 12 / 12 pass.
- `npm run spellcheck` — clean.
- All seven models refit in reporting config without warnings other
  than the LRP57 small increase in divergences noted above.

## What is *not* in scope (documented for future work)

- ITT-phase-only mediation decomposition (NDE / NIE) through L —
  LRP59+ per the original plan. β_G in mechanism models is not a
  direct-effect estimate.
- Confirming ceilings W = 90 and P = 100 (`measures.py` marks both
  `n_trials_confirmed=False`).
- Latent-variable measurement models for pre/post scores — noted as a
  future extension in the original plan.
