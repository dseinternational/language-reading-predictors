# LRP73 — is letter-sound → reading age-dependent? + interaction-phase synthesis

> [!WARNING]
> These notes were generated by an AI tool. Model numbers are from the
> **reporting** config (6 chains × 6000 draws).

Date: 2026-06-16

Third and final planned model in the interaction family. Stacked on
`feature/lrp72-phonics-route` (chain: #68 → LRP71 → LRP72 → LRP73); rebased onto
`main` once the chain merges, PR held until then.

## Question and prior expectation

Does the letter-sound (L) → word-reading (W) effect depend on **age**? Age was the
strongest *gain-side* SHAP signal. **Prior expectation, stated up front:** age is
the most between-child of all the candidate moderators (most age variation is
across children, not within a child's four waves), so the moderation was most
likely to be a between-child confound — like LRP71 (null) and LRP72 (no
super-additivity). Designed and reported to test that honestly.

## Model

LRP58 mechanism (`f_mech` GP on logit L — W is not floored) + a **covariate**
moderator (age):

    eta = ... + f_mech(logit L) + gamma_mod·z(age) + gamma_int·z(logit L)·z(age)

Machinery additions (default-off, so LRP56–58/71/72 unaffected; 15/15 factory
tests pass):

- `moderator_is_covariate` — `z(M)` from `prepared.A_std` (re-standardised on kept
  rows) instead of `z(logit(count/N))`; skips the measure guard and the count
  NaN keep-mask.
- `include_interaction` (default True) — when False, only the moderator main
  effect is added (no `gamma_int`), giving a clean nested baseline (LRP73base).

**Note on the adjustment set.** LRP58 lists `A` but, with `use_age_gp=False`
(default), did not actually condition on age. LRP73's `gamma_mod·z(age)` realises
the age adjustment **linearly** — a strict improvement for this question. `E`/`R`
confounders, `W_pre` baseline, as LRP58.

## Result (reporting; n = 157, 53 children; 20/36000 divergences, R-hat 1.00)

| coefficient | mean | 95% CI | P(>0) |
| --- | ---: | --- | ---: |
| `gamma_int` (L × age) | **−0.064** | [−0.134, +0.007] | 0.039 |
| `gamma_mod` (age main effect) | −0.086 | [−0.189, +0.016] | 0.049 |

`gamma_int` is **negative-leaning but not credible** (95% CI includes zero). No
evidence that phonics converts to reading *more* (or credibly less) strongly with
age. PSIS-LOO vs LRP73base: elpd_diff ≈ 2 (dse ≈ 1.5) — within noise.

## Within-child confound check (the decisive diagnostic)

`scripts/within_child_interaction_check.py` — OLS of logit(outcome) on z(mech),
z(moderator), product; pooled-naive vs pooled-cluster vs within-child fixed
effects (subject dummies absorb between-child variation). Interaction |t|:

| interaction | pooled-naive | within-child FE | reading |
| --- | ---: | ---: | --- |
| L × age → W (LRP73) | 1.16 | 0.79 | weak/absent both ways |
| L × E → W (LRP71) | 0.09 | 0.41 | null both ways |
| L × B → N (LRP72) | 1.02 | 0.63 (sign flips −) | weak pooled +, flips − within |

For age there is no between-child signal to collapse and none within-child either.
(The script standardises on the logit scale; it is a rough triangulation, not the
inferential model — cluster-robust SEs but no full longitudinal structure.)

## Interaction-phase synthesis

Three interaction models, one conclusion:

- **LRP71** phonics × vocabulary → reading: **null** (`gamma_int` CI spans 0).
- **LRP72** blending × letter-sound → decoding: **no super-additivity** —
  credibly negative on the logit scale but link-saturation-ambiguous on a floored
  count, and the raw pooled positive flips negative within-child; "largely
  independent."
- **LRP73** letter-sound × age → reading: **non-credible**, negative-leaning;
  within-child flat.

**Headline: in this sample, reading's contributing skills combine largely
*additively*; the raw screen-level interactions are between-child ability/age
confounds, not within-child synergies.** Each skill (letter-sound, blending,
vocabulary) makes a strong, credible *independent* contribution (the LRP58 and
LRP72 main effects), but how much one converts to reading does not credibly depend
on the others or on age once subject random effects + adjustment are in. This is a
clean, honest, publishable result and closes the loop the April plan opened
(interactions were deferred; now tested and found additive). The two-step
explore→confirm workflow earned its keep: every screen interaction that looked
real pooled dissolved under proper modelling.

A standalone interaction-phase synthesis write-up (across LRP71–73, for a paper
section) could be drafted next if useful — flagged, not done here.

## Limitations

- Age range + wait-list phase structure constrain what an "age effect" means;
  within-child age variation is limited to four waves.
- Small sample (53 children); interactions are data-hungry. Conclusions are about
  *this* sample and reading stage (word decoding), not a claim that these skills
  never interact at other developmental points.
- Reading comprehension (`yarcsi`) cannot be modelled here (67% floored; see the
  LRP72 note) — the "where does vocabulary matter for reading?" question needs an
  older / higher-reading sample.

## Engineering

- Factory: `moderator_is_covariate`, `include_interaction` (both default-off).
  Pipeline passes them through; `gamma_mod`/`gamma_int` added to diagnostics
  appropriately. New `lrp73.py`, `lrp73base.py`, registered.
  `scripts/within_child_interaction_check.py` (reusable across pairs).
  `compare_statistical_models.py`: `age_moderation_loo_compare` (shared
  `_loo_compare`). `docs/models/lrp73/index.qmd`.
- ruff + spellcheck clean; report renders; factory tests pass.

## Follow-on (not this work)

- **LRP74 — age × treatment (ITT):** "does the intervention help reading more at
  younger ages?" More decision-relevant for DSE but lower-powered (ITT phase
  n≈53) and not a top screen signal — a separate model with clear caveats.
- The standalone interaction-phase synthesis write-up noted above.
