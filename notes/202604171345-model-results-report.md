# Language and reading predictors — modelling results report

> [!WARNING]
> These notes were generated by an AI tool.

Date: 2026-04-17

## Purpose

Summarise the modelling work to date on the Down Syndrome Education
International *language and reading predictors* exploratory study.
Four primary models target two outcomes (word reading, expressive
vocabulary) across two framings (level at each timepoint, change
between timepoints):

| Model | Task | Target | n | Framing |
|---|---|---|---:|---|
| LRP01 | Word-reading gain | `ewrswr_gain` | 157 | Change between timepoints |
| LRP02 | Word-reading level | `ewrswr` | 210 | Level at each timepoint |
| LRP03 | Expressive-vocabulary gain | `eowpvt_gain` | 161 | Change between timepoints |
| LRP04 | Expressive-vocabulary level | `eowpvt` | 215 | Level at each timepoint |

This report takes the *best-fitting* configuration of each family (as
measured by pooled out-of-fold CV metrics) and describes what the four
models indicate — individually and collectively — about the predictors
of progress in language and reading for children with Down syndrome.
All data and models are preliminary; findings are for research use.

## What we have done

### Pipeline and methodology

- **Estimator**: LightGBM across all models. Adopted in April 2026
  after paired comparison against Random Forest (`notes/202604121451-
  lightgbm-model-selection.md`): accuracy within 0.04 CV RMSE of RF at
  ~30× lower wall-time, tighter importance rankings, and explicit
  zero-importance zeros that streamline feature selection.
- **Cross-validation**: `GroupKFold` grouped by `subject_id`. 51–53
  splits under the reporting configuration (essentially near-leave-
  one-subject-out), so each test fold is a single child's longitudinal
  timepoints.
- **Objective**: LightGBM `objective="mae"` for the primary exploratory
  models, tuned to minimise CV MAE via Optuna (TPE, 150 trials, 10-
  split inner GroupKFold, seed 47). The MAE objective prevents
  importance rankings from being dominated by a handful of high-
  scoring children. RMSE-tuned and quantile-tuned variants exist for
  sub-questions (absolute prediction accuracy and median prediction
  respectively).
- **Target transforms**: `log1p` for LRP02 (hard floor at 0, heavy
  right skew); signed-log for LRP01 (signed gains); none for LRP03 /
  LRP04 (mild skew). Predictions are inverse-transformed before
  scoring, so all reported metrics remain in original target units.
- **Feature selection**: iterative importance-based pruning using
  permutation importance + distance-correlation redundancy, starting
  from `Predictors.DEFAULT_GAIN` (34 predictors) or
  `Predictors.DEFAULT_LEVEL` (32 predictors). Each selection step is
  documented with a `SelectionStep` on the model class and a dated
  markdown note in `notes/`.
- **Metrics**: classic CV mean-of-fold metrics (MAE, RMSE, R², MedAE)
  plus **pooled out-of-fold metrics** (MAE, RMSE, R², MedAE computed
  on the stacked OOF predictions against the global target mean).
  Per-fold R² is unstable under near-leave-one-subject-out grouping
  because within-fold target variance is tiny; the pooled OOF R² is
  the reliable generalisation signal and is the headline metric in
  this report.
- **Diagnostics**: permutation importance, SHAP values + scatter
  plots, partial-dependence plots, distance-correlation dendrograms
  and heatmaps, mutual-information heatmaps, Spearman correlations,
  and importance × cluster tables for each fitted model.

### Sensitivity investigations

- **Time-invariant baseline measures**: 14 predictors are captured
  once at baseline (parent report or t1-only assessment) and
  replicated across timepoints in the long format — `hearing`,
  `earinf`, `hearing_c`, `vision`, `numchil`, `mumocc`, `dadocc`,
  `agebooks`, `mumedupost16`, `dadedupost16`, `bedtimeread`,
  `othertimeread`, `blocks`, `agespeak` (listed in
  `Variables.TIME_INVARIANT_BASELINES`). `GroupKFold` prevents
  test-set leakage but not training-side row-count inflation. A
  sensitivity check on LRP02 (`notes/202604171220-lrp02-time-
  invariant-weight-sensitivity.md`) found that inverse-frequency
  subject weighting leaves the importance rankings essentially
  unchanged, but that dropping `agebooks` + `agespeak` and retuning
  produces a strictly better model on every mean-based pooled
  metric. Layer-1 mitigation (labelling these features so reviewers
  can flag them during selection) is in place.
- **MAE vs RMSE tuning**: RMSE-tuned variants consistently over-fit
  at these sample sizes (in-sample R² approaches 1.0 while CV R²
  drops). MAE tuning is the primary regime; RMSE-tuned variants are
  kept for cross-checking prediction accuracy.
- **Quantile α=0.5 objective**: LightGBM's pinball loss at the
  median directly minimises MedAE and is a useful comparison point
  for typical-case prediction. On LRP04 it improves every mean CV
  metric modestly; on LRP02 with the log transform it produces the
  tightest MedAE seen in that family (2.90).
- **Cap-fix for `n_estimators`**: a bug in `RunConfig` was replacing
  (rather than capping) tuned `n_estimators` during `--config test`
  / `dev` runs. Fixed in PR #16; all variants were subsequently
  refit under honoured tuned values (`notes/202604171017-lrp02-
  refit-under-cap-fix.md`).

## Best-fitting configurations

Fitted at `--config reporting` (full CV, 50 permutation repeats, SHAP
+ PDP). The winner within each family is selected on pooled out-of-
fold R², breaking ties on pooled MAE.

| Family | Winner | n | predictors | CV MAE | CV RMSE | pooled MAE | pooled RMSE | **pooled R²** | pooled MedAE | In-sample R² |
|---|---|---:|---:|---:|---:|---:|---:|---:|---:|---:|
| LRP01 | `lrp01_log` | 157 | 6 | 2.85 | 3.35 | 2.85 | 4.14 | **0.099** | 1.92 | 0.247 |
| LRP02 | `lrp02_log` | 210 | 13 | 5.56 | 6.37 | 5.46 | 7.29 | **0.644** | 2.58 | 0.952 |
| LRP03 | `lrp03` | 161 | 11 | 5.00 | 5.75 | 4.99 | 6.66 | **0.131** | 3.98 | 0.405 |
| LRP04 | `lrp04` | 215 | 7 | 6.23 | 7.17 | 6.23 | 7.91 | **0.691** | 5.09 | 0.874 |

**Signal-strength observation.** The two **level** models (LRP02,
LRP04) generalise strongly (pooled R² 0.64 and 0.69). The two **gain**
models (LRP01, LRP03) generalise weakly (pooled R² 0.10 and 0.13). This
is consistent with gains being a much noisier target at this sample
size — level has stable between-child variance to exploit; gain has
to extract signal from much smaller differences between consecutive
timepoints.

## LRP01 — word-reading gain (`ewrswr_gain`)

### Best variant: `lrp01_log` (signed-log transform)

- **Predictors** (6): `age`, `b1exto`, `celf`, `yarclet`, `blending`, `attend`
- **Domains covered**: demographics (age); language (b1exto
  expressive vocabulary, celf basic concepts); early reading
  (yarclet letter sounds, blending phonemes); intervention
  (attend sessions).
- **Feature-selection history**: 34 → 6 predictors across six MAE-
  tuned selection steps (`notes/202604161432-lrp01-feature-
  selection-mae.md`); each step improved CV MAE.
- **Target transform**: `sign(y) × log1p(|y|)` — gains can be negative
  (19 of 157 observations), so signed-log rather than plain `log1p`.
- **Ranking agreement with raw-target primary**: identical top-6 in
  the same order — the transform slightly improves MAE (9/10 folds,
  Wilcoxon p=0.064) without perturbing interpretation.

### Permutation importance (top 6)

| Rank | Predictor | Importance | Interpretation |
|---:|---|---:|---|
| 1 | `age` | 0.113 | Older children gain more on average at this sample size |
| 2 | `attend` | 0.089 | Intervention-session attendance matters |
| 3 | `yarclet` | 0.084 | Letter-sound knowledge at baseline predicts reading gains |
| 4 | `b1exto` | 0.049 | Expressive vocabulary predicts gains |
| 4 | `celf` | 0.049 | Basic concept knowledge predicts gains |
| 6 | `blending` | 0.035 | Phoneme blending skill predicts gains |

### Direction of effects (SHAP beeswarm)

| Predictor | Direction | Notes |
|---|---|---|
| `attend` | **+** monotonic | More intervention sessions attended → more gain |
| `celf` | **+** monotonic | Higher basic-concept score → more gain |
| `yarclet` | **+** monotonic | Higher letter-sound knowledge → more gain |
| `age` | **mixed** | High-age observations produce the widest SHAP spread, including several extreme negatives; low-age cluster modestly positive. Direction is not monotonic — older children have more *variable* gain outcomes, not uniformly more. |
| `b1exto` | **+** monotonic | Higher expressive vocabulary → more gain |
| `blending` | **+** monotonic | Higher phoneme-blending score → more gain |

All five cognitive / intervention predictors act in the "more
capability / more engagement → more gain" direction expected from
theory. Age is the exception and is the one feature whose direction
warrants caution when interpreting ranked importance.

### What this model indicates

The strongest predictors of word-reading gain are **developmental
stage** (age), **intervention engagement** (attendance), and
**pre-reading skills** (letter sounds, phoneme blending). Language
measures (expressive vocabulary, basic concepts) contribute but at
roughly half the magnitude of the top-three predictors. Crucially,
*baseline word-reading score* (`ewrswr`) was dropped during selection
(importance 0.000 in Select02) — how far a child has already come in
word reading does not, in this sample, determine how much further
they gain between timepoints. That contrasts sharply with LRP03
where baseline `eowpvt` dominates gain prediction.

### Caveats

- **Low generalisation strength**: pooled R² = 0.10 means the model
  explains ~10% of between-child variance in reading gain. Individual
  prediction is unreliable; the importance *rankings* carry the
  research signal.
- **n=157 with 53-split CV**: per-fold variance is high; conclusions
  come from pooled OOF and importance stability rather than absolute
  CV values.

## LRP02 — word-reading level (`ewrswr`)

### Best variant: `lrp02_log` (log1p target transform, 13 predictors, MAE-tuned)

- **Predictors** (13): `gender`, `age`, `aptinfo`, `eowpvt`,
  `erbword`, `nonword`, `spphon`, `yarclet`, `deappvo`, `agespeak`,
  `numchil`, `agebooks`, `mumedupost16`.
- **Domains covered**: demographics (gender, age, numchil, parental
  education); language (aptinfo, eowpvt); speech (erbword, deappvo,
  agespeak); reading and phonological awareness (nonword, spphon,
  yarclet); home literacy (agebooks — age at introduction to books).
- **Feature-selection history**: 32 → 13 predictors across four MAE-
  tuned selection steps (`notes/202604161949-lrp02-feature-
  selection.md`); CV MAE dropped from 6.98 to 6.01.
- **Target transform**: `log1p(y)` — `ewrswr` has a hard floor at 0
  and a heavy right tail (median 6.5, max 64). Log-transforming the
  target homogenises error across quartiles and lifts pooled R²
  from 0.60 (primary, raw target) to 0.64.
- **Sensitivity checks**: dropping the two time-invariant baseline
  measures (`agebooks`, `agespeak`) and retuning — `lrp02_drop_age_
  measures` — produces pooled R² 0.63, better than the raw-target
  primary but worse than `lrp02_log`. The log transform is the
  bigger lever than feature reduction on this target.

### Permutation importance (top 10)

| Rank | Predictor | Importance | Interpretation |
|---:|---|---:|---|
| 1 | `yarclet` | 0.273 | Letter-sound knowledge |
| 2 | `spphon` | 0.263 | Phonetic spelling |
| 3 | `eowpvt` | 0.109 | Expressive vocabulary |
| 3 | `aptinfo` | 0.108 | Language information composite |
| 5 | `erbword` | 0.083 | Word repetition |
| 6 | `nonword` | 0.070 | Nonword reading |
| 7 | `agebooks` | 0.042 | Age at introduction to books (baseline) |
| 8 | `agespeak` | 0.040 | Age at first words (baseline) |
| 9 | `mumedupost16` | 0.036 | Maternal post-16 education |
| 10 | `age` | 0.034 | Developmental stage |

### Direction of effects (SHAP beeswarm)

| Predictor | Direction | Notes |
|---|---|---|
| `yarclet` | **+** strongly monotonic | Widest SHAP range in the plot; letter-sound knowledge is both the biggest driver *and* acts in a clean directional way |
| `spphon` | **+** strongly monotonic | Same shape as yarclet, slightly smaller range |
| `eowpvt` | **+** monotonic | Higher expressive vocabulary → higher reading level |
| `nonword` | **+** monotonic | Higher nonword-reading score → higher reading level |
| `aptinfo` | **+** monotonic | Higher language composite → higher reading level |
| `erbword` | **+** monotonic | Higher word repetition → higher reading level |
| `age` | **+** monotonic (modest) | Older → higher reading level |
| `agebooks` | **−** monotonic | **Earlier** introduction to books → higher reading level (low feature value = positive SHAP) |
| `agespeak` | **−** monotonic | **Earlier** first words → higher reading level |
| `mumedupost16` | **+** monotonic | Maternal post-16 education → higher reading level |
| `numchil` | **−** small | More siblings → slightly lower reading level (small effect) |
| `deappvo` | ≈ 0 | Very compressed; negligible direction |
| `gender` | ≈ 0 | Categorical, very small effect |

Every substantive predictor acts in the theoretically expected
direction. The two baseline developmental-milestone measures
(`agebooks`, `agespeak`) have **negative** directions — the *earlier*
these milestones happen, the higher the reading level later — which
is consistent with them being retained as markers of overall
developmental pace rather than as "time spent with books" proxies.

### What this model indicates

Word-reading level at any given timepoint is dominated by **pre-
reading and decoding skills** (letter-sound knowledge, phonetic
spelling, nonword reading) and **language** (expressive vocabulary,
information composite). Word repetition (`erbword`, a phonological
memory proxy) is the next tier. Demographic / home-literacy
indicators (`agebooks`, `agespeak`, maternal education) sit
substantially below the cognitive / linguistic cluster but are
retained because they clear the noise floor and provide
interpretive breadth.

The **strong generalisation** (pooled R² 0.644, in-sample R² 0.95)
reflects that children's word-reading scores have stable between-
child variance driven by their underlying skill profile. The model
is useful for **identifying profile dimensions** even if absolute-
score prediction should be treated with care (pooled MAE 5.46
against scores ranging 0–64).

## LRP03 — expressive-vocabulary gain (`eowpvt_gain`)

### Best variant: `lrp03` (primary, MAE-tuned, 11 predictors)

- **Predictors** (11): `eowpvt`, `deappvo`, `trog`, `aptinfo`, `age`,
  `b1reto`, `deappin`, `rowpvt`, `aptgram`, `agebooks`, `b1exto`.
- **Domains covered**: base level (eowpvt); articulation (deappvo,
  deappin); language composites (trog, aptinfo, aptgram); expressive
  / receptive vocabulary (b1reto, rowpvt, b1exto); demographics
  (age, agebooks).
- **Feature-selection history**: 34 → 11 predictors across two
  aggressive MAE-tuned cuts (`notes/202604171127-lpr03-feature-
  selection.md`); CV MAE dropped from 5.28 to 5.04.

### Permutation importance (top 11)

| Rank | Predictor | Importance | Interpretation |
|---:|---|---:|---|
| 1 | `eowpvt` | 0.248 | Baseline expressive vocabulary |
| 2 | `deappvo` | 0.111 | Articulation (vowels) |
| 3 | `aptinfo` | 0.101 | Language information composite |
| 4 | `trog` | 0.095 | Receptive grammar |
| 5 | `age` | 0.073 | Developmental stage |
| 6 | `b1reto` | 0.044 | Receptive vocabulary |
| 7 | `deappin` | 0.042 | Articulation (initial sounds) |
| 8 | `rowpvt` | 0.037 | Receptive vocabulary (standardised) |
| 9 | `aptgram` | 0.033 | Grammar composite |
| 10 | `agebooks` | 0.027 | Age at introduction to books |
| 11 | `b1exto` | 0.011 | Expressive vocabulary (block 1) |

### Direction of effects (SHAP beeswarm)

| Predictor | Direction | Notes |
|---|---|---|
| `eowpvt` | **−** strongly monotonic | **Negative**: *lower* baseline vocabulary → more gain. Classic regression-to-the-mean — by far the widest SHAP range in the plot |
| `aptinfo` | **+** monotonic | Higher language composite → more gain |
| `trog` | **+** monotonic | Higher receptive grammar → more gain |
| `deappvo` | **mixed / non-monotonic** | High feature values spread across both positive and negative SHAP tails; low values concentrate near zero. The effect is not cleanly directional — articulation contributes at extremes in both directions |
| `age` | **+** monotonic | Older children gain more |
| `deappin` | ≈ 0 / small +  | Small positive effect at high values |
| `b1reto` | ≈ 0 / small − | Weak negative-leaning signal |
| `rowpvt` | ≈ 0 | Compressed near zero |
| `aptgram` | ≈ 0 | Compressed near zero |
| `agebooks` | small −  | Later book introduction → slight negative — same direction as LRP02 |
| `b1exto` | ≈ 0 | Compressed near zero |

The dominant signal is that `eowpvt` acts in the **opposite** direction
to every other top predictor: **lower** baseline vocabulary → more
gain. This is the regression-to-the-mean pattern and means that
LRP03's top predictor is *not* saying "children with more capability
gain more" — it's saying "children with more headroom gain more". The
next-tier positive predictors (`aptinfo`, `trog`, `age`) do carry the
"more capability → more gain" interpretation, but at much smaller
magnitude.

### What this model indicates

Expressive-vocabulary gain is dominated by **baseline level**
(`eowpvt`, importance 0.25) — a classic regression-to-the-mean signal:
children with lower scores have more room to grow. **Articulation
quality** (`deappvo`, `deappin`) and **language composites** (`trog`
receptive grammar, `aptinfo` information, `aptgram` grammar) occupy
the next tier. Age contributes moderately. Home literacy (`agebooks`)
is present but secondary.

The contrast with LRP01 is informative: **baseline word-reading
score (`ewrswr`) has zero importance in LRP01** (dropped during
selection) while **baseline expressive-vocabulary score (`eowpvt`)
dominates LRP03**. This suggests vocabulary gain is a more
self-correlated, level-dependent process than reading gain at this
age range, where reading gains depend more on foundational skills
(letter sounds, phoneme blending) than on the current reading level.

### Caveats

- **Low generalisation strength**: pooled R² = 0.13 — stronger than
  LRP01 but still only explaining ~13% of between-child variance.
  Gain prediction at n=161 remains hard.

## LRP04 — expressive-vocabulary level (`eowpvt`)

### Best variant: `lrp04` (primary, MAE-tuned, 7 predictors)

- **Predictors** (7): `aptinfo`, `rowpvt`, `ewrswr`, `celf`, `age`,
  `yarclet`, `deappin`.
- **Domains covered**: language composite (aptinfo); receptive
  vocabulary (rowpvt); reading (ewrswr, yarclet); basic concepts
  (celf); articulation (deappin); demographics (age).
- **Feature-selection history**: 32 → 7 predictors across two
  steps (`notes/202604171240-lrp04-feature-selection.md`). The
  second step is **construct-driven** rather than importance-driven:
  `b1exto` (rank 1 at importance 0.164) was dropped because it is
  another expressive-vocabulary instrument — keeping it would turn
  the model into a between-tests calibration study rather than an
  identification of *non-vocabulary* predictors of expressive
  vocabulary. This cost ~0.55 CV MAE for a more interpretable
  research question.

### Permutation importance (top 7)

| Rank | Predictor | Importance | Interpretation |
|---:|---|---:|---|
| 1 | `aptinfo` | 0.403 | Language information composite — dominant |
| 2 | `rowpvt` | 0.100 | Receptive vocabulary (standardised) |
| 3 | `celf` | 0.081 | Basic concept knowledge |
| 4 | `ewrswr` | 0.076 | Word-reading composite |
| 5 | `yarclet` | 0.074 | Letter-sound knowledge |
| 6 | `age` | 0.058 | Developmental stage |
| 7 | `deappin` | 0.032 | Articulation (initial sounds) |

### Direction of effects (SHAP beeswarm)

| Predictor | Direction | Notes |
|---|---|---|
| `aptinfo` | **+** strongly monotonic | Widest SHAP range — dominant positive driver |
| `rowpvt` | **+** monotonic | Higher receptive vocabulary → higher expressive-vocabulary level |
| `ewrswr` | **+** monotonic | Higher word-reading → higher expressive vocabulary |
| `yarclet` | **+** monotonic | Higher letter-sound knowledge → higher expressive vocabulary |
| `celf` | **+** monotonic | Higher basic-concept score → higher expressive vocabulary |
| `age` | **+** monotonic | Older children have higher expressive vocabulary |
| `deappin` | **+** small monotonic | Better articulation → slightly higher expressive vocabulary |

**All seven predictors act in the expected "more capability / more
maturity → higher expressive vocabulary" direction.** LRP04 is the
most directionally clean of the four models — no mixed signals, no
regression-to-the-mean inversion, just a consistent positive effect
of every retained feature on the level of expressive vocabulary.

### What this model indicates

Expressive-vocabulary level is **overwhelmingly predicted by a single
non-vocabulary language composite** (`aptinfo`, importance 0.40 —
nearly four times any other predictor). Receptive vocabulary
(`rowpvt`), basic concepts (`celf`), and reading skill (`ewrswr`,
`yarclet`) form a secondary tier at roughly a quarter of `aptinfo`'s
importance. Age and articulation round out the model.

The **strongest pooled R² in the study** (0.69) reflects that
expressive vocabulary at a given timepoint is well-predicted by a
child's broader language profile — language abilities cluster tightly,
and a good measure of one dimension (information / composite
knowledge) nearly carries the full prediction. The construct-driven
drop of `b1exto` is the decision that makes this finding interesting
rather than tautological.

## Cross-cutting findings

### Level prediction is 5-6× easier than gain prediction

| Family | Framing | Pooled R² | In-sample R² | Gap |
|---|---|---:|---:|---:|
| LRP02 | Level | 0.64 | 0.95 | 0.31 |
| LRP04 | Level | 0.69 | 0.87 | 0.18 |
| LRP01 | Gain | 0.10 | 0.25 | 0.15 |
| LRP03 | Gain | 0.13 | 0.41 | 0.28 |

Level models generalise strongly; gain models have weak signal at
this sample size. This is structural — gains have much less between-
child variance to exploit once a child's baseline level is accounted
for. The generalisation gap (in-sample − CV) is not hugely different
between framings (0.15–0.31), but the *absolute* CV R² is lower on
the gain side because there is simply less signal.

### Which predictors matter for *what*

| Predictor domain | LRP01 (word gain) | LRP02 (word level) | LRP03 (vocabulary gain) | LRP04 (vocabulary level) |
|---|---|---|---|---|
| Reading / letter sounds | **yarclet #3** | **yarclet #1** | — | yarclet #5 |
| Phonetic spelling / decoding | blending #6 | **spphon #2** | — | — |
| Phonological memory | — | erbword #5 | — | — |
| Language composite | celf #4 | aptinfo #3 | **aptinfo #3**, trog #4, aptgram #9 | **aptinfo #1** |
| Expressive vocabulary | **b1exto #4** | eowpvt #3 | b1exto #11, b1reto #6 | (construct-dropped) |
| Receptive vocabulary | — | — | rowpvt #8 | **rowpvt #2** |
| Basic concepts | celf #4 | — | — | celf #3 |
| Articulation | — | deappvo #13 | **deappvo #2**, deappin #7 | deappin #7 |
| Reading level | (dropped) | (target) | — | ewrswr #4 |
| Baseline level of the target | (dropped) | (target) | **eowpvt #1** | (target) |
| Demographics (age) | **age #1** | age #10 | **age #5** | age #6 |
| Intervention attendance | **attend #2** | (not selected) | (dropped) | (dropped) |
| Baseline home literacy | (dropped) | agebooks #7 | agebooks #10 | (dropped) |
| Parental education | (dropped) | mumedupost16 #9 | (dropped) | (dropped) |

Ranks are within each model's selected predictor set.

**Observations**:

1. **Language composites (`aptinfo`, `celf`, `trog`)** appear in
   every model — they are consistently predictive across both
   outcomes and both framings.
2. **`yarclet` (letter sounds)** is critical for reading (top of
   LRP02, top-3 of LRP01) and a secondary predictor even for
   expressive vocabulary level (LRP04). Letter-sound knowledge
   appears to be a broad literacy and language-development signal.
3. **Articulation (`deappvo`, `deappin`)** matters far more for
   vocabulary gain than for reading. Articulation quality is linked
   to rate of change in expressive vocabulary but not to current
   reading level.
4. **`attend` (intervention attendance)** appears only in LRP01
   — word-reading gain. This is the one outcome where the
   intervention directly targets the skill being measured, and the
   one model where attendance survives feature selection.
5. **Age** is top-5 in all four models — developmental stage
   matters across the board, and its role is largest in the gain
   models (LRP01 rank 1, LRP03 rank 5).
6. **Baseline level of the target** has a strikingly different
   role in the two gain models: `eowpvt` dominates LRP03
   (importance 0.25, rank 1) but `ewrswr` was dropped by LRP01's
   feature selection. Word-reading gain is driven by foundational
   sub-skills (letter sounds, phonemes) rather than current reading
   level; expressive-vocabulary gain is driven by current vocabulary
   level.

### Direction-of-effect patterns across models

Reading the SHAP beeswarm plots (`shap_summary.png` in each model's
output directory) establishes how each predictor's *value* maps to
its *effect* on the target, which permutation importance alone
cannot tell:

1. **Level models are directionally clean.** Every retained
   predictor in LRP04 and every substantive predictor in LRP02
   acts monotonically in the expected direction (more
   capability / more maturity / earlier milestones → higher
   level). No mixed signals, no surprises.
2. **LRP03's `eowpvt` is the headline asymmetric signal.** It is
   the top predictor by a large margin *and* acts in the opposite
   direction to every other top predictor: *lower* baseline
   vocabulary → more gain (regression-to-the-mean). This is a
   crucial interpretive point — the dominant importance does not
   translate to "high baseline vocabulary predicts growth".
3. **LRP01's `age` direction is complex.** Age is rank #1 by
   permutation importance but the beeswarm shows high-age
   observations producing the widest SHAP spread, including
   several extreme negatives. The effect is not monotonic.
4. **`agebooks` and `agespeak` act negatively in LRP02.**
   *Earlier* book introduction and *earlier* first words
   associate with higher reading level — consistent with their
   role as developmental-pace indicators rather than as
   "years of book exposure" proxies.
5. **Articulation in LRP03 is non-monotonic.** `deappvo`'s SHAP
   distribution is bimodal — high articulation values appear at
   both positive and negative tails. Interpret articulation
   contributions to vocabulary gain with care.

**Take-home**: permutation importance ranks *how much* each
feature contributes; SHAP beeswarms tell you *which direction* and
*how consistently*. Both are required for sound interpretation.

### Target transforms pay off unequally

| Model | Transform | CV R² (raw → transform) |
|---|---|---|
| LRP02 | `log1p` | 0.60 → 0.64 (+0.04) |
| LRP01 | signed-log | 0.10 → 0.10 (tied) |
| LRP03 | none explored | — |
| LRP04 | none explored | — |

The LRP02 `log1p` benefit comes from its hard floor at 0 and heavy
right skew. LRP01's signed-log transform helps negative-gain
quartile MAE by ~14% in-sample but does not translate to CV R². LRP03
and LRP04 have milder skew and have not been tested with transforms
yet — a cheap sanity check for a future PR.

### Mean-based vs median-based metrics

MAE-tuning (the primary regime) produces different optima than
quantile-tuning (direct median minimisation). On LRP04, `lrp04_
quantile` improves every mean CV metric modestly but doesn't clear
α=0.05 at n=10 folds. On LRP02 with the log transform,
`lrp02_log_quantile` achieves the tightest MedAE seen in that
family (2.90). When typical-case prediction accuracy matters more
than average, the quantile variants are preferred; the MAE-tuned
primaries remain the **importance-ranking reference** because they
provide the most balanced predictor weightings.

## Limitations

1. **Sample size**. n=157–215 across the four outcomes. Near-leave-
   one-subject-out CV is the only robust option at this scale, but
   it yields high per-fold variance. Absolute CV values are reported
   with care; pooled OOF metrics and importance stability carry the
   substantive signal.
2. **Preliminary models, exploratory study**. Feature-selection
   decisions reflect current evidence and may shift as more data
   arrive. None of these models should be used for individual
   prediction or clinical decisions.
3. **Time-invariant baselines**. The sensitivity check confirmed
   that `agebooks` and `agespeak` (replicated across timepoints per
   child) can inflate their training-side importance, though the
   distortion on current LRP02 importance rankings is small. The
   systematic issue is labelled (`Variables.TIME_INVARIANT_
   BASELINES`) but not yet acted on per-model beyond LRP02.
4. **Statistical significance of comparisons**. Paired-fold tests
   at n=10 lack the power to reject α=0.05 for modest effects.
   Directional consistency (fold-win counts) and pooled OOF metrics
   are the primary comparison signals.
5. **No causal claims**. Every finding above is associational.
   Predictor importance indicates "which features help this
   GBDT explain between-child variance on this outcome", not
   "which features *cause* progress".

## Next-step candidates

- **LRP03 / LRP04 target transforms**. Cheap sanity check; milder
  skew makes the expected payoff modest but not zero.
- **LRP03 / LRP04 quantile variants**. Mirror the LRP02 / LRP04
  quantile investigations on the gain task and the raw-target
  level task.
- **LRP01 prediction variant under log transform**. Currently only
  the MAE-tuned log variant exists.
- **Sub-population sensitivity** for any model recommending
  promotion from primary to variant (e.g. `lrp02_drop_age_
  measures`): per-age-band and per-baseline-score slices to confirm
  pooled-metric improvements aren't masking regressions on
  identifiable sub-groups.
- **Time-invariant baseline treatment for LRP04**. Four of LRP04's
  seven predictors are time-varying (aptinfo, rowpvt, ewrswr, celf,
  age) and two are t1 / replicated (deappin, agebooks — *agebooks*
  is not actually in LRP04's final set; only `age` and `deappin`
  here are on the boundary). Worth re-checking under the
  `TIME_INVARIANT_BASELINES` label whether any LRP04 features
  warrant the drop-and-retune sensitivity check we did for LRP02.
- **Cross-model feature-stability analysis**. The cross-cutting
  table above is informal; a principled joint analysis would
  stabilise rankings across models where the same feature appears
  and quantify the uncertainty.

## Provenance

All fits performed at `--config reporting` on 2026-04-17 using the
hyperparameters pinned in `src/language_reading_predictors/models/
lrp01.py`, `lrp02.py`, `lrp03.py`, `lrp04.py`. Artifacts saved
under `output/models/{model_id}/` including `metrics.json`,
`cv_scores.csv`, `oof_predictions.csv`, `permutation_importance.csv`,
SHAP plots, partial-dependence plots, and feature-selection
diagnostics (distance correlation, mutual information, clustering).

Notes referenced:

- `notes/202604121451-lightgbm-model-selection.md` — LGBM
  standardisation vs Random Forest.
- `notes/202604161432-lrp01-feature-selection-mae.md` — LRP01
  feature selection from 34 → 6 predictors.
- `notes/202604161949-lrp02-feature-selection.md` — LRP02 feature
  selection from 32 → 13 predictors plus log-transform and
  select02 variants.
- `notes/202604171005-lrp01-signed-log-transform.md` — LRP01
  signed-log investigation.
- `notes/202604171017-lrp02-refit-under-cap-fix.md` — cap-fix
  refits for all LRP02 variants.
- `notes/202604171108-lrp02-quantile-objective.md` — LRP02
  quantile α=0.5 investigation.
- `notes/202604171127-lpr03-feature-selection.md` — LRP03
  feature selection from 34 → 11 predictors.
- `notes/202604171220-lrp02-time-invariant-weight-sensitivity.md`
  — sensitivity to duplicated baseline rows; `lrp02_drop_age_
  measures` variant.
- `notes/202604171240-lrp04-feature-selection.md` — LRP04
  feature selection from 32 → 7 predictors plus quantile variant.
