# Findings — mediation & mediation_multi families (how the reading gain is carried)

> [!NOTE]
> Drafted by a LLM-based AI tool (Claude Code/Opus 4.8). Numbers from the `reporting`-config refit of 2026-07-16 (see [process note](202607161130-full-statistical-refit.md)); reviewed and extended on 2026-07-17 to cover all models in the family. Preliminary. (Covers both the single-mediator `mediation` family and the two-mediator `mediation_multi` family.)

## What these models ask

Given that the intervention raises word reading (from the [ITT](202607161800-findings-itt.md) analysis), _through which skill does that effect travel_? These 16 models (13 single-mediator + 3 two-mediator) use a **g-formula decomposition** by counterfactual simulation — they re-run the fitted model under "what if the mediator behaved as it would with vs without the programme" scenarios and difference the results — to split the **total** effect into:

- **NDE / direct** — the part **not** running through the named mediator (the intervention's effect on reading holding the mediator fixed);
- **NIE / indirect** — the part that **does** run through the mediator (e.g. "the programme taught letter sounds, and letter sounds carried the reading gain").

By construction `total ≈ NDE + NIE`, and **proportion mediated** = NIE / total. The outcome is word reading (W) in 12 of the 16 models; the exceptions are `med-086/186` (off-the-floor nonword reading N) and `med-087/187` (phoneme blending B). Most quantities are on the **items scale** (raw score points); the floored nonword outcome (`med-086/186`) is reported on a **percentage-point (pp) risk-difference scale** — see the note under the table.

`med-078`, `med-186` and `med-187` are **interventional-effects** companions (labelled IIE/IDE) to `med-059`, `med-086` and `med-087` respectively: a slightly different formal interpretation of the same split that is better behaved when mediator-outcome confounders are themselves affected by treatment. Numerically they land on the same estimates as their natural-effects siblings, so we report them alongside.

Reading Bayesian output, in frequentist-friendly terms: a **95% credible interval** is the range the parameter lies in with 95% probability _given the data and the priors_ — a direct probability statement about the parameter, unlike a confidence interval. **P(effect > 0)** ("direction probability" below) is the posterior probability the true effect is positive. We grade the _direction_ probability against the project evidence ladder (#179): `inconclusive` < 0.75 ≤ `suggestive` < 0.91 ≤ `moderate` < 0.97 ≤ `strong` < 0.99 ≤ `very strong`. This family does **not** emit a ROPE ("region of practical equivalence" — a band around zero deemed too small to matter) table, so the "big enough to matter" judgement here rests on the credible interval and the item-scale magnitude rather than a formal ROPE verdict.

**Crucial caveat, stated in every model:** the direct/indirect split is **model-based, not an identified natural effect**. It assumes there is no unmeasured confounder sitting between the mediator and the outcome — a strong, untestable assumption. So NIE/NDE are **best read as a plausible decomposition under assumptions**, not as proven pathways, and no mediator-route coefficient is ever "X drives Y". `med-079` is a deliberate **negative control** (a route through grammar that the causal diagram says should carry ≈0) used to calibrate how much spurious "leakage" residual confounding produces.

## Convergence gate

All 16 models **passed** the convergence gate (R-hat ≤ 1.01, min ESS ≥ 400, BFMI ≥ 0.3, 0 divergences). Across the family R-hat maxima ranged 1.0004–1.0032 and minimum ESS 2,878–25,358, with **zero divergences everywhere**. `med-092` (the period-stacked scaffold) is the least comfortable — highest R-hat 1.0032, lowest ESS 2,878 — but still clears the gate comfortably. No models are flagged.

## Results — all models

Best estimate = posterior median; the bracketed pair is the 95% credible interval; direction probability is P(effect in its favoured direction); evidence grades that probability on the ladder above. **None of the direct/indirect (NDE/NIE) splits is causal** — every one is a model-based decomposition under a no-unmeasured-confounding assumption. Only the **total** contrast inherits an experimental warrant, and even that is qualified for `med-092` (see below). Proportion-mediated medians are shown for completeness but are **not reliable** — their posteriors are extremely heavy-tailed (means run from −0.08 to +5.6, and `med-076` sits above 1 because its direct effect is negative), so read them only as a rough "most / little of the total ran through the mediator" flag.

| Model         | Outcome / route                                | Total (95% CrI), dir. prob → evidence        | NDE / direct (95% CrI), prob → evidence                        | NIE / indirect (95% CrI), prob → evidence         | Prop. mediated (median) | Total causal?                                   |
| ------------- | ---------------------------------------------- | -------------------------------------------- | -------------------------------------------------------------- | ------------------------------------------------- | ----------------------- | ----------------------------------------------- |
| **med-059**   | W via letter sounds (L)                        | +1.95 (−0.69, +4.51), 0.929 → moderate       | +0.17 (−2.13, +2.52), 0.56 → inconclusive                      | **+1.68 (+0.38, +3.61), 0.997 → very strong**     | 0.82 (unstable)         | Yes (ITT-warranted)                             |
| med-062       | W via code-route (L+B) vs lexical share        | +1.63 (−1.00, +4.18), 0.894 → suggestive     | +0.62 (−1.92, +3.21), 0.68 → inconclusive                      | +0.92 (−0.10, +2.56), 0.961 → moderate            | 0.49 (unstable)         | Yes                                             |
| med-064       | W via L vs expressive vocab (E) — 2 mediators  | +2.00 (−0.89, +4.98), 0.913 → moderate       | −0.07 (−2.47, +2.39), 0.48 → inconclusive                      | +1.95 joint (+0.33, +4.42), 0.992 → very strong   | 0.92 (unstable)         | Yes                                             |
| med-066       | W via L vs blending (B) — 2 mediators parallel | +2.90 (+0.60, +5.30), 0.992 → very strong    | +1.26 (−0.83, +3.45), 0.88 → suggestive                        | +1.59 joint (+0.12, +3.48), 0.983 → strong        | 0.56 (unstable)         | Yes                                             |
| med-068       | W via taught expressive vocab (TE)             | +2.45 (+0.24, +4.66), 0.985 → strong         | +1.80 (−0.38, +4.09), 0.95 → moderate                          | +0.57 (−0.35, +1.98), 0.896 → suggestive          | 0.24 (unstable)         | Yes                                             |
| med-074       | W via nonword decoding (N)                     | +2.72 (+0.47, +4.96), 0.991 → very strong    | +2.59 (+0.40, +4.78), 0.99 → very strong                       | +0.07 (−0.29, +0.78), 0.699 → inconclusive        | 0.03 (unstable)         | Yes                                             |
| med-075       | W via sequential L → B → reading — 2 mediators | +2.88 (+0.53, +5.25), 0.992 → very strong    | +1.23 (−0.84, +3.41), 0.88 → suggestive                        | +1.58 joint (+0.19, +3.42), 0.985 → strong        | 0.57 (unstable)         | Yes                                             |
| **med-076**   | W (reading t4) via L at t2 (longitudinal)      | +2.44 (−1.44, +6.29), 0.895 → suggestive     | **−0.78 (−3.99, +2.49), 0.31 → inconclusive (leans negative)** | **+3.12 (+0.94, +6.04), 0.999 → very strong**     | 1.12 (unstable)         | Yes                                             |
| med-078 (IIE) | W via L — interventional companion to med-059  | +1.95 (−0.69, +4.51), 0.929 → moderate       | IDE +0.17 (−2.13, +2.52), 0.56 → inconclusive                  | IIE +1.68 (+0.38, +3.61), 0.997 → very strong     | 0.82 (unstable)         | Yes                                             |
| **med-079**   | W via grammar (T) — **negative control**       | +2.22 (−0.01, +4.44), 0.975 → strong         | +2.07 (−0.08, +4.27), 0.97 → strong                            | **+0.08 (−0.30, +0.81), 0.712 → inconclusive**    | 0.04 (unstable)         | Yes                                             |
| med-080       | W via taught receptive vocab (TR)              | +2.42 (+0.02, +4.70), 0.976 → strong         | +1.88 (−0.35, +4.07), 0.95 → moderate                          | +0.43 (−0.30, +1.86), 0.876 → suggestive          | 0.19 (unstable)         | Yes                                             |
| med-086/186   | N (off-floor, **pp scale**) via L              | +7.87 pp (−5.53, +21.52), 0.876 → suggestive | −1.11 pp (−11.86, +9.88), 0.42 → inconclusive (leans negative) | **+8.66 pp (+1.67, +17.82), 0.993 → very strong** | 0.94 (unstable)         | Yes                                             |
| med-087/187   | B (phoneme blending) via L                     | +0.73 (−0.24, +1.70), 0.931 → moderate       | +0.47 (−0.55, +1.53), 0.82 → suggestive                        | +0.23 (−0.09, +0.69), 0.921 → moderate            | 0.28 (unstable)         | Yes                                             |
| med-092       | W via L (period-stacked scaffold, #229)        | +3.03 (+0.50, +5.33), 0.989 → strong         | +2.24 (−0.17, +4.47), 0.97 → strong                            | +0.75 (+0.21, +1.55), 0.998 → very strong         | 0.25 (unstable)         | **Qualified** (ignorability, not randomisation) |

Notes on the scales and rows: `med-086/186` uses a **percentage-point** scale because the nonword outcome is heavily floored (many children score zero), so the family's floor rule recasts it as an **off-the-floor risk difference** — the change in the probability of moving off the floor, in percentage points. A +7.9 pp total there is therefore **not** comparable to the +items totals elsewhere; it means "the programme raises the chance of getting off the nonword floor by about 8 percentage points". `med-078`, `med-186` and `med-187` are the interventional-effects (IIE/IDE) companions and land on the same numbers as `med-059`, `med-086` and `med-087` — they are tabulated for symmetry (an earlier draft named `med-078` in prose but omitted its row).

## The adjusted-association detail — path-specific and direct effects

The single most important row-level fact the headline table hides is that **the direct (NDE) effect is not always positive, and in the correctly time-ordered model it is negative**:

- **Direct effects carry almost the whole total for the routes that should _not_ mediate.** In `med-074` (nonword-decoding route) the direct effect is +2.59 items (0.99, very strong) against an indirect of only +0.07 (0.699, inconclusive) — the nonword route is essentially null, floor-limited, and the reading gain sits in the direct part. The negative control `med-079` behaves the same way: direct +2.07 (0.97, strong), indirect ≈0. The taught-vocabulary routes `med-068`/`med-080` also load onto the direct part (direct +1.80 / +1.88, moderate) with small uncertain indirect shares.
- **`med-076` (L at t2 → reading at t4, correct time-ordering) has a _negative_ direct effect: −0.78 items (0.31, leans negative, inconclusive).** With the mediator measured before the outcome, the indirect L route (+3.12, 0.999) actually exceeds the total (+2.44) — i.e. under this decomposition essentially all — and then some — of the reading gain is attributed to the letter-sound pathway. This is the strongest single-model statement of the "gain travels through the code" reading, though it remains an association, not proof.
- **In the two-mediator models the joint indirect is carried entirely by letter sounds; the blending path-specific effect is slightly _negative_.** `med-064` (L vs expressive vocab): NIE_L +1.89 (+0.39, +4.10), 0.996 → very strong, vs NIE_E +0.03 (−0.69, +1.02), 0.58 → inconclusive — L dominates E decisively. `med-066` (L vs blending, parallel): NIE_L +1.76 (+0.47, +3.72), 0.998 → very strong, but **NIE_B is −0.14 (−1.14, +0.50), P(>0)=0.29 → inconclusive/leaning negative**. `med-075` (sequential L → B → reading): NIE_L +1.77 (+0.45, +3.65), 0.998, and again **NIE_B −0.15 (−1.14, +0.51), 0.29**. So although the _joint_ code-route indirect is clearly positive in both, that positive share is entirely the letter-sound path — the blending-specific contribution is small and if anything slightly negative. Read the earlier draft's "code-route mediators jointly carry a positive share" / "consistent with a decoding chain" with that in mind: do not over-credit blending.

## Longitudinal (t3) and period-1 readouts

Several models emit a secondary read using a later-timepoint or randomised-transition readout of the same decomposition. These reinforce the letter-sound story without changing the direction:

- **t3 longitudinal indirect (NIE) readouts:** `med-059` +2.96 (+0.87, +5.75) — larger and still clearly positive; `med-068` +0.83 (−0.39, +2.67); `med-074` +0.06 (−0.41, +0.97) (still ≈0); `med-079` +0.04 (−0.53, +0.94) (negative control still ≈0); `med-080` +0.75 (−0.31, +2.68); `med-086` +0.22 (items-scale readout); `med-087` +0.20 (−0.14, +0.67).
- **`med-092` period-1 (randomised-transition) readout:** the headline table uses the all-period readout (total +3.03, NIE +0.75). The cleaner **period-1** readout — the only transition where the on-intervention contrast is a genuinely randomised comparison — is **total +2.50 (+0.52, +4.54)** and **NIE +0.61 (+0.17, +1.28)**. Same direction, slightly smaller, and it is the more defensible number to quote for this model.

## Sensitivity to unmeasured mediator-outcome confounding

Because the split is only valid under no-unmeasured-confounding, each model reports a **tipping point**: the fraction of the mediator → outcome path that would have to be pure confounding (rather than a real mediator effect) to erase the indirect effect. Smaller fractions = more fragile. For the clearly-positive-NIE models: `med-059` **0.375**, `med-086` **0.45**, `med-092` **0.498**, `med-076` **0.525**. In every case `robust_over_full_sweep = False` — i.e. the NIEs are only **moderately** robust: roughly 38–53% of the mediator-outcome association would need to be confounding to null them, and the full sweep does breach that. So the letter-sound indirect route is a **repeated, direction-stable association**, not an effect that survives arbitrarily large hidden confounding.

**Instrumental-strength (IS) calibration caveat:** the g-formula cross-check against the companion cumulative-session dose model is **not available for `med-059`/`med-086` (and their IIE companions)** because that dose model, `lrp-rli-dose-083`, failed its own convergence gate in this refit. The decompositions still stand on their own priors and data, but this external calibration leg is missing for the two headline letter-sound models.

## The negative control (calibration check)

`med-079` routes the reading gain through **receptive grammar (T)** — a path the causal diagram says should carry ≈0. It does: NIE **+0.08 items (−0.30, +0.81), P(>0)=0.712 → inconclusive**, with the direct effect (+2.07, strong) carrying essentially the whole total. This is the reassuring result: the machinery is **not** manufacturing large spurious indirect effects out of residual confounding. It gives us some licence to take the clearly-positive letter-sound NIEs more seriously than we otherwise could — though, again, "more seriously" is not "proven".

## The one-paragraph story

The word-reading benefit is **most consistently associated with movement through letter-sound knowledge**: the indirect route through L is clearly positive in several independent framings (`med-059` +1.68 items, 0.997; the correctly time-ordered `med-076` L-at-t2 → reading-at-t4 +3.12, 0.999; the period-stacked `med-092` +0.75 all-period / +0.61 period-1, 0.998), and the same holds for the off-the-floor **nonword** gain (`med-086` +8.66 pp via L, 0.993). Routes through **vocabulary** (taught or standardised, receptive or expressive) carry only small, uncertain shares (`med-064` expressive NIE_E +0.03; `med-068` +0.57; `med-080` +0.43). In the two-mediator code-route models the positive joint indirect is carried **entirely by letter sounds** — the blending path-specific share is small and slightly negative. The **negative-control** route through grammar (`med-079`) correctly reads ≈0. Taken together this is consistent with — but does **not prove** — a picture in which the programme's reading benefit travels through the code (letter sounds → decoding) rather than by broadly lifting vocabulary.

## What is causal

The **total** intervention contrast inherits the randomised ITT logic for the single-outcome and two-mediator models — with one exception: **`med-092`'s exposure is per-period on-intervention under an ignorability assumption, not randomisation** (it stacks periods in the gain-factor scaffold), so its total is a weaker, adjusted quantity than a true ITT effect; the period-1 transition readout (+2.50) is the defensible causal-flavoured number there, not the all-period +3.03. The **direct/indirect split is never identified** — it is a decomposition under a no-unmeasured-mediator-outcome-confounding assumption. So read every NIE/NDE (and IIE/IDE) as "under this model", check it against the negative control (`med-079` ≈ 0) and the tipping-point sweeps (0.375–0.525, none robust over the full sweep), and never state a mediator route as an established mechanism. Positive total = intervention benefit; a positive NIE means "under the model, this much of that benefit is attributable to the mediator" — an adjusted decomposition, not a proven pathway.
