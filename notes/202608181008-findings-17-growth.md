> [!NOTE]
> Drafted by a LLM-based AI tool (Claude Code/Opus 5).

# Findings: the `growth` family — trajectories across the whole study

**Read `findings-00-overview` first.** This note covers the 4 models in the `growth` family. **Nothing here is causal.**

## The data

**Both studies.** Three models use the RLI trial across **all four timepoints** (54 children, 216 rows). One uses the historical cohort across **waves 1 to 3** (87 children, 261 rows). Data are stacked, not collapsed — the whole point is the shape of each child's trajectory.

## What the model is for

Instead of comparing timepoints two at a time, a growth-curve model fits each child their own **trajectory**: a starting level and a rate of change, both allowed to vary between children. The trajectory is linear in standardised age.

The question is then about the trajectory parameters: **does a child's baseline ability predict how _fast_ they progress**, as distinct from how high they start? Two quantities carry this — one linking baseline ability to the growth _rate_, one linking it to the _level_.

Some variants add a shared "growth tempo" factor allowing all measures' rates to move together, or an age-by-ability interaction to ask whether older-and-more-able children progress differently.

## What was found

In the **trial cohort**, the clearest relationship is between baseline non-verbal ability and growth in receptive grammar: **+0.11 logit units per SD** (89% +0.02 to +0.20). The shared-tempo variant gives the same +0.11 [+0.01, +0.21], and the age-by-ability variant gives +0.15 [+0.06, +0.25].

These are modest and consistent, and grammar is the outcome where the relationship is clearest — not reading. That is worth noting given that reading is the project's focus.

In the **historical cohort**, baseline verbal reasoning is associated with BAS word-reading growth at **−0.13 logit units per SD** (89% −0.25 to −0.01) — _negative_, meaning children with higher baseline verbal reasoning grew **more slowly** in word reading over waves 1 to 3.

## The negative result needs care, not a story

A negative ability-to-growth relationship is counter-intuitive and it would be easy to construct an explanation. Two duller possibilities should be dealt with first.

**Regression to the mean.** Children measured high at baseline include some who were having a good day. Their later scores drift back toward their true level, which reads as slower growth. Any model relating a baseline measurement to subsequent change is exposed to this, and it biases the relationship negative.

**Ceiling effects.** Children who start higher have less room to improve on a bounded test. On a measure where the strongest children approach the maximum, faster starters _must_ show smaller increments.

Both are strong candidates here, and neither can be separated from a genuine effect with these data. The interval also runs to −0.01, close to zero at one end.

The honest summary: **in this historical cohort, higher baseline verbal reasoning went with slower measured word-reading growth, by an amount consistent with regression to the mean and ceiling effects.** It is not evidence that verbal reasoning impedes reading development.

The same caution applies with the opposite sign to the trial cohort's positive grammar result, which is small.

## What these models cannot tell you

**Baseline ability is not a lever.** It was not manipulated, and the coefficient is an adjusted association.

**A linear trajectory is an assumption.** Real development is not linear, and forcing a straight line makes the estimated rate an average that may not describe any child.

**Growth rates are hard to estimate from four waves.** Separating a child's individual rate from measurement noise needs more data than most of these models have; the trial models get four points per child.

**The trial models pool across arms**, so they describe development in the study as a whole, not treatment effects.

## Model inventory

All 4 pass the convergence gate with zero divergences and are publishable: `gc-069` (joint multivariate growth), `gc-070` (shared growth-tempo factor), `gc-085` (age-by-ability interaction), and `rlm-gc-001` (historical cohort, reading-group-adjusted).

`rlm-gc-001` was initially withheld by two defects in the project's own release gate — a mis-stated exclusion count and a verdict read from the wrong place in the configuration — both fixed during this run. The fit itself was always sound; see the run note.
